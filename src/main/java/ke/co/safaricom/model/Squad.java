package ke.co.safaricom.model;

import java.util.List;

public class Squad {
    private static final int maxSize = 7;
    private int id;
    private String name;
    private String cause;
}


/*
    - add a new squad (GET, POST)
    - list all squads (GET)
    - find a squad by id/name (GET)
    - update a squad (GET, PUT/POST)
    - count number of heroes in the squad (GET)
 */